<h1> Login Page </h1>

<%= form_for User.new, url: signin_path, method: :post do |f|%>

	<p><%= f.label :name %>
		<%=f.collection_select :name, User.all, :name, :name %>
	</p>

	<p><%= f.label :password %>
		<%= password_field_tag :password %>
	</p>

	<%= f.submit "Sign In" %>

<% end %>
